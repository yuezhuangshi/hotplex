# =============================================================================
# HotPlex Server Configuration - Admin Bot
# =============================================================================
# Inherits from base configuration, only override admin-specific settings.
# =============================================================================

inherits: ./base/server.yaml

# Override: Admin bot uses debug logging for detailed diagnostics
server:
  log_level: debug
